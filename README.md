# zip_normalize_filename
Normalize file names in zip archive to utf8 NFC

## install
~~~
pip install git+https://github.com/takaakiaoki/zip_normalize_filename
~~~

## run simply
~~~
zip_normalize_filename.py <file-to-normalize.zip>
~~~
